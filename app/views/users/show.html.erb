<div class="contents row" >
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @picks.each do |pick| %>
    <div class="content_post" style="background-image: url(<%= pick.url %>);">
      <%= simple_format(pick.comment) %>
    </div>
  <% end %>
  <%= paginate(@picks) %>
</div>