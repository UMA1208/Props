<div class="contents row" >
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @tweets.each do |tweet| %>
    <div class="content_post" style="background-image: url(<%= pick.image %>);">
      <%= simple_format(pick.text) %>
    </div>
  <% end %>
  <%= paginate(@picks) %>
</div>